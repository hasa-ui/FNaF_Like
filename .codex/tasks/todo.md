# Task Log

## Plan
- [x] 仕様書と現状実装を確認し、優先度Aの実装対象を決定する
- [x] タイトル画面と操作説明画面の構成を追加する
- [x] ゲーム開始導線と画面遷移を実装する
- [x] 動作検証を実施する
- [x] 結果を報告する
- [x] コードと仕様書を比較し、バグ候補と仕様差分を特定する
- [x] 停電時判定とリスタート挙動の修正を実装する
- [x] 敵ごとの個性強化を実装する
- [x] 追加分の検証を実施する
- [x] 追加分の結果を報告する
- [x] レビュー指摘2件の原因箇所を確認する
- [x] 結果パネルの言語更新と遅延カメラ切替のガードを実装する
- [x] 優先度Aのオーディオ仕様を実装する
- [x] 追加分の検証を実施する
- [x] 追加分の結果を報告する
- [x] 現状コードから開始時刻表示とリスタート不具合の原因を特定する
- [x] 開始時刻表示とリスタート挙動を修正する
- [x] 日本語切替と操作感改善を実装する
- [x] 優先度Aのジャンプスケア演出を追加する
- [x] 追加分の検証を実施する
- [x] 追加分の結果を報告する

## Progress Log
- 2026-03-17: `仕様書.md` を確認。優先度Aのうち、導線不足を直接解消できる `タイトル画面` と `操作説明画面` を先行実装対象に決定。
- 2026-03-17: `index.html` にタイトル画面と操作説明画面のオーバーレイを追加。`game.js` に `title/howto/playing` の状態遷移を実装し、開始前はゲームループが動かない構成に変更。
- 2026-03-17: `style.css` にメニュー用のレイアウトとモバイル時の縦並び調整を追加。
- 2026-03-17: 追加確認を実施。停電時の襲撃判定が `office` のみ、`Restart Shift` がタイトルへ戻らない点を修正対象に決定。次の優先機能として `敵ごとの個性強化` を実装予定。
- 2026-03-17: `game.js` を更新。停電時は `hallL` / `hallR` を含むオフィス付近からも襲撃成立とし、`Restart Shift` は初期状態であるタイトル画面へ戻すよう変更。
- 2026-03-17: 優先度Aの `敵ごとの個性強化` を追加。Rust は接近が遅い代わりに侵入猶予を短縮、Moth はカメラ多用で活性化、Grin は左右をランダムにまたぎ読取表示を不安定化。
- 2026-03-17: 再確認を実施。開始時計算の式が壊れており 0 分時点で `6 AM` 表示になること、`Restart Shift` がタイトル遷移化されていてシフト再開にならないことを今回の修正対象に決定。
- 2026-03-17: `game.js` を整理し、開始時刻表示を修正。`Restart Shift` はタイトル遷移ではなく即時にシフトを再開する挙動へ戻した。
- 2026-03-17: 日本語/英語切替を追加。主要 UI、ステータス、ヒント、メニュー文言、ボタン文言を切替可能にした。
- 2026-03-17: モニター開閉時の短い入力ロックと画面フリップ演出を追加し、モニター展開中はドア操作を無効化して操作感を調整。
- 2026-03-17: 仕様書の優先度Aから `ジャンプスケア演出` を追加。敗北時にフラッシュと顔表示を挟んでから結果表示へ遷移する。
- 2026-03-17: レビュー指摘を確認。言語切替時に結果パネルの文言が更新されない点と、遅延モニター切替がゲーム終了後にも走る点を今回の修正対象に決定。
- 2026-03-17: 言語切替時に結果パネルも再翻訳されるよう `endTitleKey` / `endDescKey` を保持する構成へ修正。遅延モニター切替は timeout を追跡し、ゲーム終了後は反映しないようガードした。
- 2026-03-17: 仕様書の優先度Aから `オーディオ仕様` を追加。Web Audio による環境音、カメラ開閉音、ドア音、敵接近音、停電音、ジャンプスケア音、勝利音を実装し、上部に音声オン/オフ切替を追加。

## Verify Log
- 2026-03-17: `node --check game.js` 実行。終了コード 0 で構文エラーなし。
- 2026-03-17: `git diff --check -- index.html style.css game.js .codex/tasks/todo.md` 実行。終了コード 0 で whitespace 問題なし。
- 2026-03-17: `node -e "...eval(fs.readFileSync('game.js','utf8'))..."` による簡易 smoke test 実行。終了コード 0、初期化時クラッシュなし。
- 2026-03-17: `git diff --check -- game.js .codex/tasks/todo.md` 実行。終了コード 0 で追加差分にも whitespace 問題なし。
- 2026-03-17: 追加の `node -e "...eval(fs.readFileSync('game.js','utf8'))..."` smoke test を実行。`clock` の初期表示が `12 AM` であることを確認し、終了コード 0。
- 2026-03-17: `git diff --check -- index.html style.css game.js .codex/tasks/todo.md` を再実行。終了コード 0。
- 2026-03-17: `node -e` による追加 smoke test を実行。`win -> 言語切替` で結果パネルが英語へ更新されること、モニター遅延切替中に `gameState='lose'` へ変えても `statusKey` と `cameraOpen` が上書きされないことを確認。
- 2026-03-17: `node --check game.js` と `git diff --check -- index.html game.js .codex/tasks/todo.md` を再実行。終了コード 0。

- [ ] コミット 9e313d9 の差分を確認する
- [ ] 影響箇所と仕様整合性を検証する
- [ ] レビュー結果を報告する

## Progress Log (review 9e313d9)
- 2026-03-17: レビュー開始。差分、仕様書、既存タスクログを確認。
- 2026-03-17: `node --check game.js` を実行し、構文エラーがないことを確認。
- 2026-03-17: オーディオ追加差分と状態遷移（開始/再開/勝敗/停電/言語切替）をレビューし、このコミット起因で確実に再現する不具合は未検出。

## Plan (2026-03-24 仕様書更新)
- [x] 現行の `仕様書.md` と `index.html` / `style.css` / `game.js` を比較し、現行仕様を棚卸しする
- [x] 現行実装に合わせて `仕様書.md` を更新する
- [x] 更新後の仕様書を基に `ロードマップ.md` を作成する
- [x] 差分と整合性を確認し、検証結果を記録する

## Progress Log (2026-03-24 仕様書更新)
- 2026-03-24: `仕様書.md`、`index.html`、`style.css`、`game.js` を確認。既存仕様書は「現行仕様」と「将来方針」が混在しているため、今回は現行実装に合わせた仕様整理を主目的とする。
- 2026-03-24: 現行実装の主要仕様を確認。単一 Night のブラウザ向け試作であり、`title` / `howto` / `playing` / `win` / `lose` の状態、5 カメラ、3 敵、電力管理、音声切替、日英切替、簡易音響、ジャンプスケア、Threat Readout 補助表示を備える。
- 2026-03-24: `仕様書.md` を全面整理。将来構想を分離し、現行実装の画面構成、状態遷移、数値仕様、敵挙動、UI、音、制約を実装準拠で記述した。
- 2026-03-24: 更新後の仕様書を前提に `ロードマップ.md` を新規作成。Phase 1 から Phase 4 までの開発順序、各フェーズの目的、主要項目、完了条件を整理した。

## Verify Log (2026-03-24 仕様書更新)
- 2026-03-24: 仕様棚卸しのため `sed` / `rg` で `仕様書.md`、`index.html`、`style.css`、`game.js` を確認。現行仕様の根拠をコードから採取開始。
- 2026-03-24: `git diff -- 仕様書.md ロードマップ.md .codex/tasks/todo.md` で差分内容を確認。仕様書が将来構想中心から現行仕様中心に再整理され、ロードマップが新規追加されたことを確認。
- 2026-03-24: `git diff --check -- 仕様書.md ロードマップ.md .codex/tasks/todo.md` を実行。初回は `仕様書.md` の末尾空白 2 件を検出し、修正後に再実行して終了コード 0 を確認する。
